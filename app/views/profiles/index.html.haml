.grid.text-center.m-10.grid-cols-3
  - @profiles.each do |profile| 
    .rounded-md.bg-gray-800.w-96.h-96.mt-10.hover:shadow-lg.overflow-auto
      %h1.text-2xl.font-bold.m-2
        = link_to profile.name, user_path(profile.user.username)
      %p 
        = link_to profile.intro, user_path(profile.user.username)